# UI

Vite + React + TypeScript frontend for Rincon Fire v1.

## Routes

- `/` dashboard
- `/compare` cross-model risk comparison table
- `/areas/:areaId` area details
- `*` not found

## Run

```bash
npm install
npm run dev
```

Use `.env.example` to configure `VITE_API_BASE_URL`.
